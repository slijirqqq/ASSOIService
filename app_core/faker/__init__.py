from faker import Faker

# Import our own providers in order to register Faker providers automatically

# plain Faker instance with all our providers registered
fake = Faker()

# fake.add_provider(ExampleProvider)
